import React from "react";

interface TodoProps {

}

const TodoList: React.FC<TodoProps> = () => {
    return (
        <div>
            todo list
        </div>
    );
};

export default TodoList;